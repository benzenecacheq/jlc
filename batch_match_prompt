Match ALL the construction/lumber items below against the parts database using ALL available information.

ITEMS TO MATCH:
`items_text`

`training_examples`

PARTS DATABASE (`database_name`):
`column_info`

Database format (pipe-separated values):
`db_text`

MATCHING INSTRUCTIONS:
1. For EACH item above, find the best matches from the database
2. Use ALL columns for matching, not just the description
3. Look for exact dimension matches, material equivalents, length conversions
4. Consider unit compatibility and handwritten abbreviations
5. `training_instruction`

Respond with JSON array where each match includes the item index:
[
  {
    "item_index": 1,
    "part_number": "exact item number from database",
    "description": "full database description",
    "confidence": "exact|high|medium|low",
    "reason": "specific explanation"
  }
]

Return up to `max_matches` best matches per item, ordered by confidence. If no reasonable matches for an item, omit it from the response.
